---
name: steward-decisions
description: 项目防漂移决策红线 D1–D18 速查。动手维护项目（改架构、改提示词、改状态机制、改知识沉淀）之前必调：哪些设计已被拍板、哪些红线不可触碰、哪些坑已被记录在案。
---

# 防漂移决策红线（DECISIONS 速查）

这些决策由项目所有者拍板，**不得在未向玩家指出的情况下推翻**。若维护中发现某决策与现实冲突，先报告玩家，说明冲突点，等指示——不要自行改设计。

| # | 决策 | 一句话 |
|---|---|---|
| D1 | 插件组合成自定义 RP 模式 | 不做独立应用 |
| D2 | 先不做导演草稿板 | 保持最小 |
| D3 | Author 与状态记账异步 | 正文优先，状态后台补 |
| D4 | Chronicler 是正经 Agent | 不是提取器，是有判断力的智体 |
| D5 | 世界状态结构动态演进 | Agent 可按剧情新增字段 |
| D6 | 玩家矫正是自然时序无锁 | Last-Write-Wins |
| D7 | 知识体系全面 Skills 化 | 替代酒馆 Lorebook |
| D8 | 允许受控的动态知识沉淀 | 新设定沉淀但不导致世界观崩塌 |
| D9 | 分支映射原生 Session.fork | 不做自定义分支结构 |
| D10 | 与 dsh-synapse 协同 | 地图交给它 |
| D11 | 摘要由独立 Agent 负责 | 按轮触发，玩家可关 |
| D12 | 记忆能力外置 | EverOS 方向 |
| D13 | 卡包格式后定 | 不受旧格式约束 |
| D14 | 不兼容酒馆卡生态 | 靠 AI 单向转译 |
| D15 | 个人玩具定位 | 单机单人，拒绝企业级复杂度 |
| D16 | 反八股句式清单 | persona 句式禁令 + anti-cliche 技能分工 |
| D17 | 条件注入 v1 | 裁决式注入块，仅 number/boolean |
| D18 | 统一 AgentPromptContract | 结构统一、内容分流；Author 的创作授权永不进数据管道 |

## 工程红线（违者真机爆炸）

1. 绝不发明会话事件类型——状态寄存在已知 `user/message` 的 `source`；
2. 不在陌生 context 上属性读取服务——用 `ctx.get(name)`；
3. 注入上下文只追加、绝不 replace——前缀缓存是性能命脉；
4. 绝不自建 HTTP 服务器 / 前端整站 / 数据库引擎；
5. 系统提示词逐字节恒定，易变数据只走 user message 尾部。

## 已知边界与观察项

- 世界线地图只画 live 会话（重启后未点开的支线不进图，点开即回）；
- 副驾驶 storage domain 曾有一条记录在 flush 后消失（一次性，未定位）；
- 卡包技能与 preset 是超集关系：管家技能包会出现在所有卡包 preset 中。
