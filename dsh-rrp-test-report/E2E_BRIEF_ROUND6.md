# dsh-rrp 第六轮真机抽查任务书 (Round-6 Spot Check Brief)

> **委托对象**：具有 Chrome 真机操作能力的测试 Agent
> **编写日期**：2026-09-20
> **对应代码基线**：`main` @ `3785de2`（验证工作区另含未提交格式/文档改动；本轮范围仍为 issue #18 卡包界面 L1+L2）
> **归档要求**：结果写入 `dsh-rrp-test-report/TEST_REPORT_R6.md`，截图命名 `r6-XX-名称.png` 存入 `dsh-rrp-test-report/screenshots/`

---

## 1. 范围与已有覆盖

本轮针对 **#18 卡包自带界面（舞台 Stage）**。实施当日已完成一轮开发者自验（见 `D:\projects\issue18-stage-plan.md` §七），本轮的任务是**独立复验 + 补齐开发者没做的压力/边界项**。

开发者已验（仍需抽查复核，不必重跑全套）：舞台页签出现、米娅与雁门两卡声明式面板渲染、`when` 条件面板正确隐藏、`ask_copilot` 预填不发送、`correct_state` 落 flag 且账本归因 player、沙箱页面 `SecurityError` + CSP 双拦截、iframe 内按钮 → 状态 8→10 → 沙箱与 L1 同步刷新。

**不在本轮**：#16/#25/#28/#32/#33/#34 已验功能（除非发现被 #18 碰坏）。

## 2. 环境准备

1. `cd D:\projects\dsh-rrp && pnpm run build`；
2. **杀 3099 旧进程**；启动务必清代理环境变量（R5 教训：Clash SOCKS 模式会让宿主 llm 静默空响应，极易误诊为代码回归）：
   `env -u ALL_PROXY -u all_proxy -u HTTP_PROXY -u http_proxy sh -c 'nohup dsh --profile rp-dev --port 3099 --no-open > /tmp/dsh-host-r6.log 2>&1 &'`
3. 宿主日志应有 `card UI route armed at /dsh-rrp/card-ui`；
4. **注意卡副本遮蔽**：`~/.dsh/.dsh-rrp/cards/maid-heiress/` 若存在（R4 测试残留），它会盖住仓库版官方卡。测仓库版米娅 UI 前先把它移开（**移动而非删除**，测完还原），或改用雁门卡测。

## 3. 抽查内容表（T42 ~ T48）

| 编号 | 用例 | 操作 | 判定标准 |
|---|---|---|---|
| **T42** | 舞台页签与空态 | ①开一张**没有 `ui/`** 的卡（或临时改名）②开雁门卡 | ①舞台显示「本卡未声明界面」，不报错、不白屏；②显示「孤灯客栈 · 案头」+ 全部面板 |
| **T43** | 声明式面板随剧情刷新 | 雁门局连推 3 轮，每轮观察舞台：好感 gauge、在场住客（新角色是否自动出现）、关系网 | 无需手动刷新即跟随；Chronicler 新增角色自动进入 `characterCard`；无闪烁/无重复节点 |
| **T44** | 条件面板翻转 | 米娅局把好感从 <40 改到 ≥40（舞台按钮或右侧栏矫正） | 「升温阶段」gauge **从无到有**出现；改回 <40 时消失 |
| **T45** | 两原语边界 | ①点 `ask_copilot` 按钮 ②点 `correct_state` 按钮 | ①副驾驶打开且输入框有内容**但未发送**；②右侧栏世界状态出现该 flag，「最近变更」记为**玩家**就地矫正（不是副驾驶） |
| **T46** | 沙箱隔离（安全红线） | 米娅 `console.html` 页面自检两格读数；另在宿主控制台执行 `document.querySelector('iframe[sandbox]').contentDocument` | 页面自检显示「父窗口被拒（SecurityError）」与「外联被 CSP 拦截」；父侧 `contentDocument` 为 `null`。**任一失守 = P0，立即停测回报** |
| **T47** | 卡页面不随状态重建 | 在 `console.html` 的输入框打字但不点按钮 → 触发一次状态变更（点「递毛巾」或右侧栏改好感）→ 回看输入框 | 输入框内容**保留**（srcdoc 未被重算）；页面滚动位置保留 |
| **T48** | 流式期间刷新成本（开发者未做，重点） | 开一轮长正文生成，打字机期间观察舞台：有无掉帧、卡顿、面板闪烁；记录宿主底部 tok/s 与是否明显劣化 | 生成期间舞台可交互、不卡顿；若卡顿，记录复现轮次与耗时对比（对照组：切到「对话」页签再跑一轮） |

**加分项（有时间再做）**
- T49 世界线读档后舞台是否回到该切面（打开一条旧支线会话，看好感 gauge 是否等于那一轮的数值）；
- T50 坏 manifest 的报错体验：故意把 `ui/manifest.json` 写坏（如 panel id 重复），确认舞台给出可读提示、卡仍能玩、宿主日志有带卡名的错误。

## 4. 缺陷记录

- 编号从 **DEF-07** 起（DEF-05→#34、DEF-06→#35 均已关闭）；
- **P0 红线**：沙箱逃逸（卡页面能读宿主 DOM / 能外联 / 能开第三条写路径）；
- P1：舞台把宿主主区拖垮（T48 严重卡顿）、状态刷新丢失卡页面内容（T47 失败）；
- 已知非缺陷：舞台页签对所有 RP 会话常驻显示（无 `ui/` 时显示空态）——与「世界线」页签同现状，属有意取舍。

## 5. 报告要求

总结评定必须回答：
1. 「卡只写声明、框架管机制」这条路在真机上是否真的够用（哪些展示需求 L1 表达不了）；
2. 沙箱 L2 的安全模型是否可信任到能对外分发卡包；
3. 常驻订阅（`useProjection`）在流式高频更新下的实际成本；
4. 与酒馆式「聊天流内嵌状态栏」相比，页签式舞台的体验得失。
