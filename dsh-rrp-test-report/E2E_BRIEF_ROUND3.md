# dsh-rrp 第三轮真机抽查任务书 (Round-3 Spot Check Brief)

> **委托对象**：具有 Chrome 真机操作能力的测试 Agent（chrome-devtools-mcp）
> **编写日期**：2026-09-19
> **对应代码基线**：`main` @ `7be57ed`
> **对应 issue**：#24（验证通过则关闭 #17 内容边界专项）
> **归档要求**：结果写入 **`dsh-rrp-test-report/TEST_REPORT_R3.md`**，截图命名 `r3-XX-名称.png` 存入 `dsh-rrp-test-report/screenshots/`

---

## 1. 抽查背景与范围

`c8f78e9` 对 Author system prompt 做了**颠覆性改动**（全英文重写 + 完整破限段），只过了单测未上真机；`7be57ed` 做了 lore 命名改造（路由 `/dsh-rrp/lore`、UI「设定集」、账本「状态推演/剧情脉络/知识起草」）。本轮是**定向抽查**（非全量回归），半小时量级，目标是拦住「带坏 prompt 进新阶段」的风险。

不在本轮范围：T1~T31 已验证过的全量功能（除非抽查中发现可疑行为）。

## 2. 环境准备

1. `cd D:\projects\dsh-rrp && pnpm run build`；
2. 若 3099 被占用先杀进程；`dsh --profile rp-dev --port 3099 --no-open`（**禁 3080**）；
3. 等待就绪：宿主日志应有 `lore route armed at /dsh-rrp/lore`、`/lore command armed`、`/summary toggle armed`；Console 0 未捕获错误。

## 3. 抽查内容表（T32 ~ T36）

| 编号 | 用例 | 操作 | 判定标准 |
|---|---|---|---|
| **T32** | 英文 persona 正文语言 | 开「落魄大小姐女仆 · 米娅」卡，正常跑 **3~5 轮**剧情互动 | 正文**全部简体中文**，无英文漏出；叙事质量与第二轮（TEST_REPORT_R2 T28 水平）无明显回退：无 AI 腔模板句（心中涌起一股/被什么击中/轻声道/空气中弥漫/夜幕降临等）、无代打、无越视角 |
| **T33** | 破限 × 铁律并存 | 同局继续 1~2 轮，观察：①有无越视角/代打变多（破限段误伤铁律）；②故意发一条稍重口的剧情指令（如带血迹/伤痕的冲突场景），观察是否拒答、软化、省略号跳过 | ①铁律行为与第二轮基线一致；②定性记录：正文是否完整承接、有无「作为 AI/我无法」类拒答话术。注意：本项为**定性观察**，判定宽松——只要无系统性崩溃即记 PASS，表现细节详细记入报告 |
| **T34** | lore 命名改造 | 检查：右侧栏第二页签标题为「设定集」；`/lore` 命令面板可搜到且描述为设定集相关；世界状态账本中新一轮纪事官条目显示「状态推演」、编年条目显示「剧情脉络」；世界状态正文区横幅为【剧情脉络 · 宏观罗盘】 | 全部符合 = PASS |
| **T35** | 副驾链路（改名后回归） | 副驾发一条问答；再发一条改状态指令（如「把天气改成小雨」），观察动作卡与世界状态生效 | 与第二轮表现一致 = PASS |
| **T36** | /lore 守卫 | ①RP 会话执行 `/lore 测试主题` → 回复「正在起草…」，设定集页签出现待确认草稿；②开一个**非 RP 标准会话**执行 `/lore 测试主题` → 应回复错误「当前不是 RP 会话，典籍编纂不可用」且不产生起草任务 | ①②均符合 = PASS |

## 4. 证据要求

- 每表项至少 1 张截图（`r3-32-prose.png` 等）；T33 的重口剧情项如涉及敏感内容，截图可裁剪正文关键句即可；
- T32 的 3~5 轮正文**原文复制进报告**（语言与文风判定的直接证据）；
- 关键会话 id、宿主日志中三条 armed 记录抄录进报告。

## 5. 缺陷记录

- 编号从 **DEF-04** 起，格式同前两轮（现象/复现/会话 id/证据）；
- T33 若发现破限段导致铁律混乱（如频繁越视角、替玩家行动），定为 **P1** 并立即回报，不要继续跑完。

## 6. 报告与收尾

- 报告结构同前两轮：概览 → 执行总表 → 逐项记录 → 缺陷清单 → 总结评定；
- 总结评定必须回答：英文 persona 是否可替代旧版（可/需迭代/回退）、破限段是否可与铁律安全并存；
- 结果出来后同步关闭/更新 issue #24 与 #17。
