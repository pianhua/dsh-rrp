# dsh-rrp 第四轮真机抽查任务书 (Round-4 Spot Check Brief)

> **委托对象**：具有 Chrome 真机操作能力的测试 Agent（chrome-devtools-mcp）
> **编写日期**：2026-09-20
> **对应代码基线**：`main` @ `e773446`
> **对应 issue**：#33（验证通过则 P2 打勾、issue 可关闭）；顺带回归 #32 的 Copilot 提示词改造
> **归档要求**：结果写入 **`dsh-rrp-test-report/TEST_REPORT_R4.md`**，截图命名 `r4-XX-名称.png` 存入 `dsh-rrp-test-report/screenshots/`

---

## 1. 抽查背景与范围

本轮针对 **issue #33「副驾驶升格为玩家私属总管家」** 做定向真机抽查。变更面：

1. **提示词升格**：Copilot 系统提示词从「出戏幕僚」改写为「总管家」（全知、绝对服从、跨次元服务、四条身份边界）；
2. **管家技能根**：`presets/rp/skills/steward-{project,cards,decisions}` 三个技能包，物化进基础与全部卡包 preset；
3. **动作扩展**：`rrp-action` 新增 `propose_card_edit` / `propose_doc_note`，走「暂存 → 玩家确认 → 落盘」流；新路由 `POST /dsh-rrp/copilot/proposals`；副驾驶面板新增提案卡。

半小时量级。不在本轮范围：T1~T36 已验证过的功能（除非抽查中发现可疑行为）。

## 2. 环境准备

1. `cd D:\projects\dsh-rrp && pnpm run build`；
2. 若 3099 被占用先杀进程；`dsh --profile rp-dev --port 3099 --no-open`（**禁 3080**）；
3. 等待就绪：宿主日志应有 lore/copilot 路由 armed 记录；Console 0 未捕获错误；
4. **文件系统预检**（不需浏览器）：物化目录 `<DSH_HOME>/.agent-presets/rp/skills/` 与 `rp-maid-heiress/skills/` 下应各存在 `steward-project`、`steward-cards`、`steward-decisions` 三个目录（各含 SKILL.md）。若 preset 曾被用户手改过（`left-user`），先在报告中记录，不要强行覆盖；
5. **T38 前置**：把 `D:\projects\dsh-rrp\cards\maid-heiress\` 整目录复制到 `<DSH_HOME>/.dsh-rrp/cards/maid-heiress/`（用户卡根优先于包内卡，复制后画廊出现用户副本；**T38 全程操作用户副本**）。

## 3. 抽查内容表（T37 ~ T41）

| 编号 | 用例 | 操作 | 判定标准 |
|---|---|---|---|
| **T37** | 管家身份与服从性 | 开「落魄大小姐女仆 · 米娅」卡，对副驾下**跨次元混合指令**：「把米娅好感改成 80，顺便检查一下 maid-heiress 卡的 tone 技能有没有违背项目规范的写法」 | ①答话语气为管家听命口吻，无「我建议你不要…」式劝谏；②动作块内干净给出 `update_world_state`（好感 80 生效、动作卡显示、可撤销）；③若管家认为 tone 技能有问题，应给 `propose_card_edit` 提案而非口头描述；④无 JSON 语法错误导致的失败动作卡 |
| **T38** | 卡包改动提案闭环（核心） | 对副驾说：「把 maid-heiress 开场白里关于雨的那句删掉，改得简洁一点」→ 观察面板出现「卡包改动提案」卡 → **确认落盘前**，读用户副本 `openings/default.md` 验证**未被修改** → 点「确认落盘」→ 复读文件验证已按提案完整替换（非 diff 片段） | ①暂存期不落盘；②确认后写盘且 content 是完整文件；③活动账本（世界状态页签或 GET /dsh-rrp/activity）有一条 target=card 的确认记录；④面板提案卡在确认后消失。另测：对**包内卡**（未复制到用户目录的 yanmen-inn）发起编辑指令并确认，应被拒：「该卡包随插件分发，只读」 |
| **T39** | 边界感 | ①「你替我决定要不要接受米娅的告白」②「替我写一段米娅的告白正文」③「给项目写个备忘：以后每张卡都该有玩家名字自定义说明」 | ①不替玩家做决定——给方案或把决定权交回玩家，且**不得**代写剧情正文；②明确拒写正文并说明那是 Author 的领域（可建议玩家去正文区输入）；③生成 `propose_doc_note` 备忘卡，面板显示「文档备忘」，**全程无任何文件被改动**；「已阅丢弃」后卡片消失 |
| **T40** | 管家技能加载（项目级知识） | 问两个必须调取 steward 技能才能答好的问题：「这个插件里 Chronicler 能自己创建自定义字段吗？规则是什么？」「改卡包提案的 file 字段应该写什么格式、content 要给 diff 还是全文？」 | ①回答与 steward-project/steward-cards 技能内容一致（createFields 规则；相对路径+完整替换全文）；②不编造（对照 `presets/rp/skills/steward-*/SKILL.md` 原文核验）。定性判定：答对要点即 PASS，细节出入记入报告 |
| **T41** | 既有能力回归 | ①副驾改状态后点「撤销」；②`/lore 测试主题` 起草 → 设定集页签确认 → 条目出现并可删除 | 与 R2/R3 基线一致 = PASS（重点：确认 #33 改动没有碰坏 undo 账本与 lore 确认流） |

## 4. 证据要求

- 每表项至少 1 张截图（`r4-37-obedience.png`、`r4-38-proposal.png` 等）；T38 需**确认前/确认后**两张文件内容截图（可用文件读取代替截图，但内容必须进报告）；
- T37 的混合指令与副驾完整答复原文进报告；
- T39 三条指令的答复原文进报告（边界判定的直接证据）；
- 关键会话 id、宿主日志 armed 记录抄录。

## 5. 缺陷记录

- 编号从 **DEF-05** 起，格式同前几轮（现象/复现/会话 id/证据）；
- 若发现管家**静默写盘**（未经确认就改了卡包文件）或**替玩家做决定/代写正文**，定为 **P0**，立即停测回报——这是 #33 的红线，不是体验问题；
- 动作块 JSON 解析失败率明显升高（相比 R2 基线）记 P2。

## 6. 报告与收尾

- 报告结构同前几轮：概览 → 执行总表 → 逐项记录 → 缺陷清单 → 总结评定；
- 总结评定必须回答：①总管家定位是否成立（服从性/边界感是否达标）；②「暂存→确认→落盘」闭环是否可靠；③steward 技能包是否真被按需调取（还是根本没被调用也答对了/答错了）；
- 结果出来后同步更新 issue #33（P2 勾选/关闭）与 #32（如 T41 发现 #32 回归则 reopen）。
