# DEF-04 复验简报（单场景重测）

> **委托对象**：Chrome 真机测试 Agent
> **基线**：`main` @ `f700436`（persona 末尾新增 Output Discipline 铁律）
> **前置**：`pnpm run build` → `dsh --profile rp-dev --port 3099 --no-open`（禁 3080）
> **结果写入**：追加到 `dsh-rrp-test-report/TEST_REPORT_R3.md` 第 7 节「DEF-04 修复复验」

## 复验步骤（对应第三轮报告的 DEF-04 复现路径）

1. 用 `rp-maid-heiress` 预设开新会话（模型 deepseek-v4.1-flash，同原缺陷渠道）；
2. 发送原触发输入：
   `我愣了愣，赶紧把她让进屋里：「外面冷，快进来吧，先换双拖鞋。对了，刚才说什么和牛？」`
3. 观察整段流式输出与最终渲染，再续跑 **2~3 轮**常规互动扩大采样；
4. 逐段检查正文。

## 判定标准

| 检查项 | 通过条件 |
|---|---|
| 英文漏出 | 全文（含流式中间态）**零英文单词**进入正文区，无构思/自纠/计划旁白 |
| 重复输出 | 无同段内容因自我纠偏重复输出两次 |
| 文风基线 | 不回退到旧 AI 腔模板句（心中涌起/被什么击中/轻声道等） |
| 铁律 | 无代打、无越视角（防回归） |

四项全过 = **PASS**，#24 可关闭；任一失败 = FAIL，记录原文片段回报，缺陷升为 P1 并重新分析根因。

## 截图

- `r3-def04-reverify-turn1.png`（Turn 1 完整正文）
- 如有续跑：`r3-def04-reverify-turn2.png`
