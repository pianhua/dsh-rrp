# MANUAL_TEST.md — 实机验收清单

> 自动化测试不含浏览器渲染与真实模型；下列项目必须由所有者在 `rp-dev` 实例上人工确认。
> 每次改动相关区域后重跑本表。

## 0. 启动实例

```bash
cd /d/projects/dsh-rrp && pnpm run build
dsh --profile rp-dev --port 3099 --no-open
# 打开日志末尾打印的 http://127.0.0.1:3099/?token=… 链接
```

启动日志应包含：

- `activity route armed at /dsh-rrp/activity`
- `active-card projection registered (key 'rrpCard')`
- `card routes armed` / `card start route armed`
- `RP skills visible (0): (none)`（基础 `rp` 模式**不带**任何卡包设定）
- `card preset 'rp-maid-heiress' skills (6): maid-apartment, maid-cecilia, maid-family, maid-mia, maid-tone-rules, maid-world-setting`

## 1. 主题（原生亮/暗，P0 已撤回）

- [ ] 刷新页面后整体就是 **DSH 原版观感**（无暖纸色、无强制衬线、无强制行高）；
- [ ] 设置 → 外观切换**亮/暗/跟随系统**，插件面板（展厅、世界状态、沉浸）全部跟着变且可读；
- [ ] 卸载插件后宿主外观**本来就没被改过**（当前插件不再注册任何 ctx.theme override）。

## 2. 卡片展厅（P2）

- [ ] 左侧边栏出现「**卡片展厅**」导航图标；
- [ ] 点开后主区显示卡包列表，含「**女仆大小姐**」；
- [ ] 选中后右侧显示：简介、标签、玩家角色、6 个世界知识、开场白全文；
- [ ] 点「刷新」不报错。

## 3. 开卡开局（P2，关键）

- [ ] 点「**开始这一局**」后切到一个新会话，标题为「**女仆大小姐**」；
- [ ] 开场白以**正文第一条**出现（若是折叠的「序章」上下文行，说明宿主拒绝了 `assistant/message`，记录之）；
- [ ] 右侧「世界状态」立即显示：米娅（好感 6）、场景「平民公寓 · 门口」、3 条事件；
- [ ] 「最近变更」显示 **卡包 · 初始状态**。

## 4. 卡包设定是否真的进了作者（关键）

发一句行动（如「我去开门」），然后在轨迹/请求检查里确认作者的前置上下文包含：

- [ ] `【当前卡包 · 设定基准】`（卡名、玩家角色、世界核心、人设规则）；
- [ ] `【世界状态 · 事实基准】`；
- [ ] 作者正文**遵守卡包设定**（不凭空换世界观、不自称「纪事官/作者」）。

## 5. 技能检索（D7）

- [ ] 剧情触及家族/公寓/塞西莉亚时，模型能通过 `skill` 工具调取到 `maid-*` 技能；
- [ ] 轨迹里能看到 `available_skills` 含这 6 个技能。

## 6. 状态归因与矫正（二测修复）

- [ ] 一轮结束后，右侧「最近变更」出现 **纪事官 · 已更新状态** + 人类可读摘要；
- [ ] 就地修改某个数值并「保存矫正」，下一轮作者按新值起笔；
- [ ] 保存后「最近变更」出现 **你 · 已就地矫正**。

## 7. 已知风险

- [ ] **秘密剧透**：卡包设定含「米娅是财阀千金」这一核心秘密。若它出现在**聊天正文**（而非只在轨迹/请求检查里），立即报告——需要改为纯 Skill 承载。
- [ ] 开场白 `assistant/message` 若被宿主拒绝，会回退成 plugin notice；两种情况都请记录。

## 8. UI 精修（2026-09-17，本轮，待实机确认）

**卡片展厅**

- [ ] 顶栏：左侧「卡片展厅」标题 + 卡包数量徽标；右侧搜索框 + 刷新按钮（悬停有 tooltip）；
- [ ] 左列：卡包行有**彩色封面方块**（由卡 id 决定，稳定不变）+ 名称 + 简介；选中行有高亮背景与描边；
- [ ] 搜索框输入关键字（如「女仆」）能实时过滤；输入无匹配词显示「没有匹配的卡包」；清空恢复；
- [ ] 右侧预览：大封面 + 标题 + 简介 + 标签胶囊 + 玩家角色行 + 世界知识（6）列表 + 开场白引用块；
- [ ] 底部操作条常驻：左侧状态、右侧主按钮「**开始这一局**」（带播放图标）；
- [ ] 加载瞬间为骨架屏，不闪烁、不报错；点「刷新」按钮可重载。

**世界状态（右栏）**

- [ ] 顶部：标题 + 未保存时出现「未保存」胶囊；
- [ ] 「最近变更」账本卡片：纪事官运行中显示**动画状态点** + 「纪事官正在推演…」；
- [ ] 场景、角色、物品、事件四段均为**卡片化**编辑：字段上方小标签 + 原生输入框；段标题带数量徽标与「添加」按钮；
- [ ] 每个条目右上角有垃圾桶图标（tooltip「删除」）；
- [ ] 底部常驻保存条：保存中按钮显示 loading 图标；保存成功后状态文案更新、未保存胶囊消失；
- [ ] 空状态显示「尚未建立世界状态…」提示卡。

**观感与回归**

- [ ] 亮/暗两种模式下，上述面板的边框、文字、按钮对比度正常，无「白色方块/黑底黑字」；
- [ ] 面板交互（点击、聚焦、悬停）与宿主其他面板一致（圆角胶囊按钮、输入框聚焦变品牌色）。

**会话可读性（2026-09-17 关键修复）**

- [ ] 打开任意历史 RP 记录：**不再出现红色「历史加载失败 / unknown to this harness」横幅**；
- [ ] 点「开始这一局」后**无需手动刷新**：开场白立即作为正文第一条出现，提示词注入不再顶在最前；
- [ ] 新会话的日志里**不再有 `rrp/*` 事件**（状态改寄存在 `user/message` 的 `source.rrp`）；
- [ ] 「最近变更」仍能显示纪事官/玩家的归因（现在由 `GET /dsh-rrp/activity` 轮询）。

> 结果请追加到下节表格；本轮尚未由浏览器 Agent 全量确认。

## 9. 知识沉淀（D8，本轮，待实机确认）

- [ ] 右侧出现「**典籍**」tab；
- [ ] 在「世界状态」面板所在会话里点「**沉淀最近的新设定**」（或输入 `/lore 青丘狐族`），几秒后出现**待确认草稿**（名称/描述/正文）；
- [ ] 草稿期间会话日志**没有**任何写入；点「**丢弃**」后草稿消失、列表不变；
- [ ] 再生成一次并「**确认写入**」，列表出现该技能；会话日志新增普通 `user/message`，其 `source.rrp.sediment.kind` 为 `add`，不再生成运行时 Skill 目录；
- [ ] **下一轮**在轨迹里确认 `available_skills` 出现该技能（渐进披露：只在需要时加载正文）；
- [ ] **同卡双存档隔离**：用同一张卡再开始一局，新 Session 的 `available_skills` **看不到**刚才那条；在新局写入同名技能不会冲突；
- [ ] **fork 继承与分流**：从确认后的切点 fork，子局能看到该技能；随后只在父局新增/删除一条，子局不变，反向亦然；
- [ ] **旧数据迁移**（有旧 sidecar 时）：首次创建该旧局的 RP agent 后，列表恢复旧条目，日志出现一个 `snapshot`，原目录改名为 `.legacy.bak`；模拟 append 失败时源目录保留；
- [ ] **只新增**：对同一会话重复确认同名技能会被拒绝（400）；
- [ ] 「典籍」面板删除该技能后，下一轮它从 `available_skills` 消失；
- [ ] 材质/报错：非法名称、超长正文被拒且面板给出可读错误。

## 10. 存档与 Workspace（本轮，待实机确认）

- [ ] 展厅在宿主有 Workspace 时显示紧凑选择器，选项名称与原生左侧栏一致；无 Workspace 服务或无条目时选择器不占位；
- [ ] 选择 Workspace 后开始一局，新 Session 出现在该原生 Workspace 下；不选择时创建未分组 Session；
- [ ] 同一张卡连续开始两次得到两个独立 Session，状态、摘要开关和典籍互不影响；
- [ ] `/summary off` 只影响当前 Session；新开同卡存档默认开启，从关闭后的切点 fork 则继承关闭状态；
- [ ] 卡片展厅、Workspace 下拉、按钮、空态与错误态均使用 DSH 原生 token/控件，窄窗口不溢出或遮挡。

---

## 实机验收结果（2026-09-16，浏览器测试 Agent）

| 检查大项 | 结果 | 记录 |
| :--- | :---: | :--- |
| 1. 主题（P0） | **PASS** | 暖纸色 `#f6f1e7` + 衬线 + 1.95 行高生效；明暗均可读、可逆 |
| 2. 卡片展厅（P2） | **PASS** | 图标/列表/简介/标签/玩家角色/6 技能/开场白完整；刷新无异常 |
| 3. 开卡开局 | **FAIL → 已修** | 报 `cannot get property "remote.agentPresets" without inject`；见下方修复 |
| 4. 设定入作者 | **PASS** | 前置上下文含设定基准与事实基准；正文第三人称、不代打、不自称作者 |
| 5. 技能检索（D7） | **PASS** | `available_skills` 含全部 6 个 `maid-*`，模型主动 `skill` 载入全部 6 篇 |
| 6. 状态归因与矫正 | **PASS** | 纪事官归因准确；就地改好感 7→20 后，次轮纪事官按 20 推进到 25（Last-Write-Wins） |
| 7. 已知风险 | **PASS** | **零剧透**（秘密只通过行为细节体现）；开场白被宿主原生接受为**正文第一条** |

### 缺陷与修复

- **现象**：卡片展厅点「开始这一局」报 `cannot get property "remote.agentPresets" without inject`。
- **根因**：Cordis Remote 代理是**命名空间服务**，只声明 `'remote'` 不够，必须显式声明 `'remote.agentPresets'`
  （宿主 `dsh-client-ui-agent-preset/lib/client.js:1397` 即如此声明）。
- **修复**：`src/client/index.ts` 的 `inject` 数组加入 `'remote.agentPresets'`（提交 `69611c6`）。
- **状态**：已复测通过（点「开始这一局」直接成功）。

---

## 沉浸视图（P3）验收

- [ ] 会话视图 Tab 出现「**沉浸**」（与「对话」「轨迹」并列）；
- [ ] 切到「沉浸」后，正文按**居中单栏**重排（跟随宿主字体，不再强制衬线）；
- [ ] 玩家行动显示为左侧竖线的**斜体行**，作者叙述为**正文段**；
- [ ] 工具 / 系统 / 上下文节点**不出现**在沉浸视图；
- [ ] 切回「对话」一切正常（沉浸视图是**纯增量**，不影响宿主渲染）；
- [ ] 若暂无可读正文，显示「这一局还没有正文…」而不报错。

### 结果（2026-09-16，浏览器测试 Agent）：**全部 PASS**

| # | 项 | 结果 |
| :--: | :--- | :---: |
| 1 | Tab 入口（对话 / 沉浸 / 轨迹） | **PASS** |
| 2 | 版式：720px 居中单栏；行动 italic + 左边框；正文 17px / 2.05 / 两端对齐 | **PASS** |
| 3 | 无噪：系统提示词、上下文注入、4 次 Skill 卡、思考过程全部过滤 | **PASS** |
| 4 | 切回「对话」完全复原 | **PASS** |
