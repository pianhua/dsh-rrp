# Commits to skip in `git blame` — pure reformatting, no logic.
# Enable once per clone with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# (or per-command: git blame --ignore-revs-file .git-blame-ignore-revs <file>)
# GitHub honours this file automatically for its blame views.

47f4c3eb05be9304a548434e4257836a739467cb
